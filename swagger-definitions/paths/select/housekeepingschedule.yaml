get:
  summary: ""
  operationId: searchHousekeepingSchedule
  description: By passing in the appropriate options, you can search for a HousekeepingSchedule in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: HousekeepingScheduleID
      description: ID of the HousekeepingSchedule you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/HousekeepingScheduleItem'
    400:
      description: Bad Request