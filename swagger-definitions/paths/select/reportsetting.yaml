get:
  summary: ""
  operationId: searchReportSetting
  description: By passing in the appropriate options, you can search for a ReportSetting in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: ReportSettingID
      description: ID of the ReportSetting you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/ReportSettingItem'
    400:
      description: Bad Request