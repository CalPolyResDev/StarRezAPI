get:
  summary: ""
  operationId: searchEntryElectronicIdentity
  description: By passing in the appropriate options, you can search for a EntryElectronicIdentity in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: EntryElectronicIdentityID
      description: ID of the EntryElectronicIdentity you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/EntryElectronicIdentityItem'
    400:
      description: Bad Request