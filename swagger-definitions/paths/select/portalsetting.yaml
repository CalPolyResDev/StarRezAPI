get:
  summary: ""
  operationId: searchPortalSetting
  description: By passing in the appropriate options, you can search for a PortalSetting in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: PortalSettingID
      description: ID of the PortalSetting you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/PortalSettingItem'
    400:
      description: Bad Request