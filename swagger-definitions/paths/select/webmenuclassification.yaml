get:
  summary: ""
  operationId: searchWebMenuClassification
  description: By passing in the appropriate options, you can search for a WebMenuClassification in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: WebMenuClassificationID
      description: ID of the WebMenuClassification you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/WebMenuClassificationItem'
    400:
      description: Bad Request