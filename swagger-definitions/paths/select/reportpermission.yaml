get:
  summary: ""
  operationId: searchReportPermission
  description: By passing in the appropriate options, you can search for a ReportPermission in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: ReportPermissionID
      description: ID of the ReportPermission you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/ReportPermissionItem'
    400:
      description: Bad Request