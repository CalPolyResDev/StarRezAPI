get:
  summary: ""
  operationId: searchEventMealPlan
  description: By passing in the appropriate options, you can search for a EventMealPlan in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: EventMealPlanID
      description: ID of the EventMealPlan you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/EventMealPlanItem'
    400:
      description: Bad Request
post:
  summary: ""
  operationId: searchEventMealPlanXML
  description: By passing in the appropriate xml, you can search for a EventMealPlan in the system
  consumes:
    - application/xml
  produces:
    - application/json
  parameters:
    - in: body
      name: Query
      required: true
      description: An XML query on the EventMealPlan table
      schema:
        type: string
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/EventMealPlanItem'
    400:
      description: Bad Request
