get:
  summary: ""
  operationId: searchTaskTemplateItem
  description: By passing in the appropriate options, you can search for a TaskTemplateItem in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: TaskTemplateItemID
      description: ID of the TaskTemplateItem you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/TaskTemplateItemItem'
    400:
      description: Bad Request