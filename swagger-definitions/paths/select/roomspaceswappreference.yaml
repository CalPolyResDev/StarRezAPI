get:
  summary: ""
  operationId: searchRoomSpaceSwapPreference
  description: By passing in the appropriate options, you can search for a RoomSpaceSwapPreference in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: RoomSpaceSwapPreferenceID
      description: ID of the RoomSpaceSwapPreference you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/RoomSpaceSwapPreferenceItem'
    400:
      description: Bad Request
post:
  summary: ""
  operationId: searchRoomSpaceSwapPreferenceXML
  description: By passing in the appropriate xml, you can search for a RoomSpaceSwapPreference in the system
  consumes:
    - application/xml
  produces:
    - application/json
  parameters:
    - in: body
      name: Query
      required: true
      description: An XML query on the RoomSpaceSwapPreference table
      schema:
        type: string'
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/RoomSpaceSwapPreferenceItem'
    400:
      description: Bad Request
