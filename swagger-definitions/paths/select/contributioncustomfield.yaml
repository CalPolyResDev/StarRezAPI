get:
  summary: ""
  operationId: searchContributionCustomField
  description: By passing in the appropriate options, you can search for a ContributionCustomField in the system
  consumes:
    - application/json
    - application/xml
  produces:
    - application/json
  parameters:
    - in: query
      name: ContributionCustomFieldID
      description: ID of the ContributionCustomField you want to look up
      required: false
      type: integer
  responses:
    200:
      description: Search results matching criteria.
      schema:
        type: array
        items:
          $ref: '#/definitions/ContributionCustomFieldItem'
    400:
      description: Bad Request