type: object
#required:
properties:
  EmailFromAddressPermissionID:
    type: integer
    format: int32
    description: Email From Address Permission
  EmailFromAddressID:
    type: integer
    format: int32
    description: Email From Address
  AccessTypeEnum:
    type: string
    format: enum
    description: Access Type
  SecurityGroupID:
    type: integer
    format: int32
    description: Security Group
  DateModified:
    type: string
    format: datetimeseconds
    description: Date Modified
